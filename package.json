{
  "name": "topheavy-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "docs:build": "npm run docs:typedoc -w @topheavy/website && npm run docs:build -w @topheavy/website",
    "docs:dev": "npm run docs:typedoc -w @topheavy/website && npm run docs:dev -w @topheavy/website"
  }
}
